// home_service.dart
// Optional service layer for home screen data fetching.
// Expand this to make real API calls when backend is ready.
class HomeService {
  // Future: fetch categories, products, banners from API
}
